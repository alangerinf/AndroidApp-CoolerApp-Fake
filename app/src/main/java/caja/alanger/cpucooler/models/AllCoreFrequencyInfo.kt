package caja.alanger.cpucooler.models

class AllCoreFrequencyInfo(coreCount: Int) {

    val freqs: IntArray = IntArray(coreCount)
    val minFreqs: IntArray = IntArray(coreCount)
    val maxFreqs: IntArray = IntArray(coreCount)

}
